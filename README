钉钉监控说明：

    配置文件：
        Config.py 里面的的配置 全局引用
    参数：
        【钉钉配置】
        webhook -> 钉钉机器人的api地址
        keyName -> 钉钉机器人的关键词

        【监控硬盘】
        threshold -> 设置监控硬盘使用量

        【进程监控】
        server_name -> 设置监控的进程名称 区分大小写 要与进程名称一致

        【本地端口监控】
        tcp_port -> 设置本机监控的端口

        【网站监控】
         weburl -> 设置网站的URL

        【Telent监控】
        telnet_data -> 多台服务器进行端口探测


安装环境:
    1. 环境python3 pip3
    2. pip3 install -r requirements.txt
    3. 配置config文件
    4. 主程序main.py
    5. 使用crontab 设置计划任务
